# All API keys and credentials are configured through the editor UI.
# See Settings → Integrations in the editor.
#
# The only optional env var is NEXTAUTH_URL for OAuth redirect URL:
# NEXTAUTH_URL=http://localhost:3000
